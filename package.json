{
  "name": "elisee-mobile-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "playwright": "^1.61.0"
  }
}
